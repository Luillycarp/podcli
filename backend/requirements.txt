# Transcription — faster-whisper es más liviano que openai-whisper
# (no requiere PyTorch, usa CTranslate2)
faster-whisper>=1.0.0

# Speaker diarization (opcional)
# pyannote.audio>=3.1.0
# speechbrain>=1.0.0

# Face detection (opcional, para smart cropping)
opencv-python-headless>=4.8.0
numpy>=1.24.0

# Thumbnails
Pillow>=10.0.0

# Utilities
python-dotenv>=1.0.0
